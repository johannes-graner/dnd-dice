/*
package dnd_dice
import character._

object sim {
    def expect(
        n: Int,
        spell: (Attacker, Target, String) => Int,
        attacker: Attacker,
        target: Target,
        adv: String
    ): Double = {
        (
            List
            .fill(n)(spell(attacker, target, adv))
            .sum
            : Double
        ) / (n: Double)
    }
}
*/
